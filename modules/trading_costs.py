"""왕복 거래비용의 **단일 출처**. 시장마다 다르다 — 그 구분이 이 파일의 요점이다.

2026-08-25 이전에는 이 값이 저장소 안에 **여섯 개**로 갈라져 있었다:
`services.py` 0.30 · `kosdaq_intraday_vwap_guard.py` 0.33 ·
`report_regime_signal_shadow.py` 0.6 · `report_kr_swing_candidate.py` 0.3 ·
`report_kospi_normal_pead_shadow.py` 0.6 · `report_nasdaq_session_tape.py` 0.25.

두 배 차이가 나는 값들이 같은 원장을 채점했고, 폐기선 판정은 그중 가장 낙관적인
0.30 을 썼다. **어느 것도 실측이 아니었다.**
"""

# ── KR (KOSPI · KOSDAQ) ──────────────────────────────────────────────────
# 운영자 실측(2026-08-25). **슬리피지 포함** — 수수료·세금만이 아니다.
#
# 이건 실현 **평균**이고, 뒤의 노출은 균일하지 않다 — [W2] 실측으로 KOSDAQ k=1 은
# 거래의 16.6% 가 신호일 상한가이고 종목당 자본이 3.2배 집중된다. 그 부분집합의
# 한계비용이 평균보다 높아도 평균은 0.215 로 유지된다. 원장에 체결가·주문크기
# 필드가 없어 이 코드베이스에서는 분해할 수 없다.
KR_ROUNDTRIP_COST_PCT = 0.215

# ── US (NASDAQ) ──────────────────────────────────────────────────────────
# 🔴 **실측이 아니다.** KR 실측치를 여기 쓰면 안 된다 — 세금 구조가 다르다
# (한국 증권거래세에 해당하는 것이 미국에는 없다). 아래 값은 `report_nasdaq_session_tape.py`
# 가 원래 쓰던 가정치를 그대로 옮긴 것이고, 운영자 실측으로 대체돼야 한다.
US_ROUNDTRIP_COST_PCT = 0.25

# 하위호환 별칭. 새 코드는 시장을 명시한 이름을 써라.
ROUNDTRIP_COST_PCT = KR_ROUNDTRIP_COST_PCT
